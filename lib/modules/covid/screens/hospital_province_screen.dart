import 'package:flutter/material.dart';

class HospitalProvinceScreen extends StatelessWidget {
  const HospitalProvinceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
