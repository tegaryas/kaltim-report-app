import 'package:flutter/material.dart';

class EmergencyCallDataValidateScreen extends StatelessWidget {
  const EmergencyCallDataValidateScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
